import 'package:flutter/material.dart';

class ViewUsersView extends StatelessWidget {
  const ViewUsersView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(height: 289, width: 564, color: Colors.amber),
    );
  }
}
